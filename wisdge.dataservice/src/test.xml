<?xml version="1.0" encoding="UTF-8"?>
<workset name="Sys">
	<!-- 
	<sql name="project.loadAll" dbType="MYSQL/SQLSERVER/ORACLE">
		<![CDATA[
		]]>
	</sql>
	 -->
	<sql name="user.get">
		<![CDATA[
SELECT * FROM T_USER WHERE USERNAME = ?
		]]>
		<where>
			<if test="status != null">
				STATUS = ${status}
			</if>
			<if test="filter != null">
				AND FULLNAME LINK ${filter}
			</if>
		</where>
	</sql>
</workset>
